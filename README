LKDEV
=====

Build rootfs thing for GNU/Linux kernel development stuff:

	cd buildroot # Oh, needs buildroot too.
	make O=/path/to/build BR2_EXTERNAL=/path/to/lkdev power9le_defconfig
	cd /path/to/build
	make
